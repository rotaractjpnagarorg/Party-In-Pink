import React from 'react';
import { DonatePage } from './DonatePage.js';

/**
 * Unified Sponsorship & Donations Page.
 * Renders the integrated deck sponsorship packages, PiP Pay contribution form,
 * in-kind partnerships, and leadership team.
 */
export const SponsorshipPage: React.FC = () => {
  return <DonatePage />;
};
